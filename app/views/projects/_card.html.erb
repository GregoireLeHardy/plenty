<div class="col-xs-12 col-sm-4">
<!-- image url has to change with the url of the profile pic -->
<div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_tag project.photo.key, width: 200, height: 150, crop: :fill %>')">
  <div class="card-category"><i class="fa fa-usd" aria-hidden="true"></i>
    <%= project.id %></div>
    <div class="card-description">
      <h2><%= project.name %></h2>
      <p><%= project.short_description %></p>
      <p><%= project.long_description %>€</p>
    </div>
    <%= link_to project_path(project), class: "card-link" do %>
    <% end %>
  </div>
</div>
